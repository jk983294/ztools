file(GLOB ZERGSrc "*.cpp")
add_library(zerg STATIC ${ZERGSrc})
target_link_libraries( zerg stdc++fs z)
set_property(TARGET zerg PROPERTY POSITION_INDEPENDENT_CODE ON)
install(TARGETS zerg RUNTIME DESTINATION lib)